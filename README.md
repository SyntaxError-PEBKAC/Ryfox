# Ryfox
Firefox for Ryzen 9000 CPUs. Custom ZEN5 Windows Firefox build: clang-cl + lld-link, Release, PGO (MOZ_PGO=1) + full LTO, tuned for Zen5 (znver5). Built from source (no artifacts). ccache enabled for faster rebuilds.
